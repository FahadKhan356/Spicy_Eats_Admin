import 'dart:ui';

class MyAppColor {
  static const backupNumColor = Color(0xFFF5F6FE);
  static const secondary = Color(0xFFA8E6E6);
  static const iconGray = Color(0xFF767676); // Fixed from 0xff7f67676 (invalid)
  static const primary = Color(0xFF282828); // Fixed from 0xff28280 (invalid)
  static const primaryBg = Color(0xFFF5F5FD);
  static const secondaryBg = Color(0xFFCCCCFB);
  static const barBg = Color(0xFFCA3E3C); // Fixed from 0xffca3sec (invalid)
}
